import { MutableRefObject } from 'react';
import { Options, SendMessage } from './types';
export declare const attachSharedListeners: (webSocketInstance: WebSocket, url: string, optionsRef: MutableRefObject<Options>, sendMessage: SendMessage) => () => void;
